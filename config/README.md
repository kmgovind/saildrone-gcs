# Configuration

MAVROS, MAVLink routing, ROS QoS, RFD900+, and Foxglove configuration belongs
here. Secrets and device-specific credentials must not be committed.

The supported runtime is ROS 2 Lyrical on Ubuntu 26.04. Configuration must not
depend on packages from ROS Rolling.
